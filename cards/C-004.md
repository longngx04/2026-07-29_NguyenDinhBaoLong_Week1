# C-004 — Automate the two scans and document the Week-1 demo

status: todo
deps: C-001, C-002, C-003
implements: FR4, FR5

## Scope

Add the aggregate `make scan` interface, a GitHub Actions workflow that checks out submodules, runs both scans, validates the raw report files, and uploads them as an artifact. Write the README/Week-1 guide with architecture, safe Docker commands, scan/CI commands, raw output locations, documented WebGoat endpoints, and limitations; do not introduce Week-2 normalization or any active testing.

## Allowed files

- `Makefile`
- `.github/workflows/security-scan.yml`
- `README.md`
- `docs/week1.md`
- `cards/C-004.md`

## Verify (run these before calling the card done)

- [ ] `make scan` exits 0 and leaves both `results/raw/opengrep.json` and `results/raw/findsecbugs.sarif` parseable.
- [ ] The workflow YAML is syntactically valid and uses submodule checkout plus a raw-artifact upload step that names both reports.
- [ ] After push, one GitHub Actions run is green and its artifact contains exactly the two documented raw reports.
- [ ] README commands and endpoint paths match the final Compose/Make interfaces.

## Done-evidence (world-state proof, named BEFORE building)

The public repository README enables a new contributor to run the local Week-1 demo, and a green GitHub Actions run exposes a downloadable raw-artifact bundle containing the exact OpenGrep JSON and FindSecBugs SARIF reports.

## Evidence (paste the actual proof here when done)

(empty until done)
