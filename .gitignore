
# /flow run-state (generated; safe to ignore)
MODE
PROJECT_TYPE
.flow/

# Generated scanner evidence is retained by CI artifacts, not committed.
results/raw/

# Operator-provided project briefs are kept local rather than published with the demo.
*.pdf

# Active flow session state is local-only.
flow/.lock
