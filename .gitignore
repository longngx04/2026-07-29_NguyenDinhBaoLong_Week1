# /flow run-state (generated; safe to ignore)
MODE
PROJECT_TYPE
.flow/

# Internal buildflow / planning artifacts — not part of Week-1 mentor handoff.
flow/
cards/
docs/ground-truth.md
DESIGN.md
RETRO.md

# OpenGrep binary is downloaded by scripts/scan-opengrep.sh before image build.
scanner/opengrep

# Operator-provided project briefs are kept local rather than published with the demo.
*.pdf

# Python bytecode
__pycache__/
*.py[cod]

# Local raw scan output (regenerate with make scan). Normalized findings are committed.
results/raw/
