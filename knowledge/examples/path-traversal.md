---
title: Example path traversal
tags: [example, path-traversal, access-control]
---

# Example — Path traversal

User supplies `../../etc/passwd` as a file name. Normalize paths and confine reads to an allowlisted directory.
